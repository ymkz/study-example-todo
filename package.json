{
  "private": true,
  "license": "MIT",
  "version": "0.0.0",
  "name": "study-example-todo",
  "description": "study-example-todo-description",
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend",
    "install": "run-p install:*",
    "start:backend": "npm run start --prefix backend",
    "start:frontend": "npm run start --prefix frontend",
    "start": "run-p start:*",
    "slide:backend": "fusuma start -d backend",
    "slide:frontend": "fusuma start -d frontend",
    "slide:engineer": "fusuma start -d engineer"
  },
  "devDependencies": {
    "fusuma": "^1.8.0",
    "npm-run-all": "^4.1.5"
  }
}
